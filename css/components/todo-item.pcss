.todo__item-title {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 0.5rem;

    .todo__item-title-editable {
        flex-grow: 1;
        max-width: calc(100% - 4rem);
    }
}